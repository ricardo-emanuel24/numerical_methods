'''import matplotlib.pyplot as plt
import numpy as np

x = np.linspace(-1, 4, 50)  # Create a list of evenly-spaced numbers over the range
plt.plot(x, np.sin(x), 'o-', x, np.cos(x),'^-')       # Plot the sine of each x point
plt.show()
'''
